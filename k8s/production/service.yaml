apiVersion: v1
kind: Service
metadata:
  labels:
    name: indonesia-platform
  name: indonesia-platform
  namespace: climate-watch
spec:
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  selector:
    name: indonesia-platform
